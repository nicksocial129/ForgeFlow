import { Interceptor } from '@grpc/grpc-js';
import { Middleware, MiddlewareRequestHandlerContext } from '../../config/middleware/middleware';
import { MomentoLoggerFactory } from '../../';
export declare function middlewaresInterceptor(loggerFactory: MomentoLoggerFactory, middlewares: Middleware[], middlewareRequestContext: MiddlewareRequestHandlerContext): Interceptor;
